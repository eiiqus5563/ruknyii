"use client";

import { FirstAppSetup } from "@/components/first-app-setup";

/**
 * /app/new — Create a new app.
 * Uses the same wizard flow as the first-app setup.
 */
export default function CreateAppPage() {
  return <FirstAppSetup />;
}
